[tools]
ruby = "4.0.6"

[tasks.dependencies]
description = "Install locked Ruby dependencies"
run = [
  "bundle check || bundle install --jobs \"$(nproc)\" --retry 3"
]

[tasks.database]
description = "Prepare development and test databases"
run = [
  "bin/rails db:prepare",
  "RAILS_ENV=test bin/rails db:prepare"
]
