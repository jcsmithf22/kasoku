<%# locals: (space:, todo:) -%>

<%= form_with model: [space, todo], data: { controller: "reset-form", action: "turbo:submit-end->reset-form#reset" } do |form| %>
  <%= form.text_field :name, id: "new_todo", placeholder: "Add todo", autocomplete: "off" %>
<% end %>
