<%= turbo_stream_from Current.user %>

<div>
  <h1>Spaces</h1>

  <%= render "new", space: @space %>

  <ul id="spaces" role="list" class="flex flex-column gap margin-block txt-align-start">
    <%= render partial: "space", locals: { user: Current.user }, collection: @spaces %>
    <li>No spaces yet</li>
  </ul>
</div>
