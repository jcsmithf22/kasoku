<%# locals: (space:, user:) -%>

<li id="<%= dom_id space %>">
  <%= link_to space do %>
    <span><%= space.name %></span>
    <% if user.id != space.owner_id %>
      <span><%= space.my_role(user.id) %></span>
    <% end %>
  <% end %>
</li>
