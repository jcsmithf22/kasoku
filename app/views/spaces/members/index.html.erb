<%= turbo_stream_from "#{@space.slug}_members" %>

<div>
  <h1>Members</h1>

  <%= render "controls", space: @space %>

  <ul id="members">
    <li>
      <span><%= @space.owner.full_name %></span>
      <span>owner</span>
    </li>
    <%= render partial: "member", locals: { space: @space }, collection: @members %>
    <li>No members yet</li>
  </ul>
</div>
