<div class="p-2 flex flex-col gap-2 sm:gap-6 sm:p-6">

  <div class="">
    <div class="h-24 w-full"></div>
    <h1 class="font-semibold text-3xl sticky top-0 py-4 text-center">Members</h1>
    <div class="h-24 w-full"></div>
  </div>

  <%= render "form", space: @space, member: @member %>

  <%= link_to "Return to space",
  space_path(@space.slug),
  class: "link link-hover text-center mt-4" %>

</div>
